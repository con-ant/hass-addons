# Keep the build context small. Only the Dockerfile and run.sh are needed inside
# the image build; everything below is add-on metadata, docs, or assets consumed
# by HA Supervisor, not the image.
.git
.gitignore
*.png
README.md
CHANGELOG.md
config.yaml
translations/
